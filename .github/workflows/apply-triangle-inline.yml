name: Apply inline date triangle patch

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply_triangle_inline.py'
      - '.github/workflows/apply-triangle-inline.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply patch
        run: python scripts/apply_triangle_inline.py
      - name: Validate JavaScript
        run: node --check static/app.js && node --check public/static/app.js
      - name: Run tests
        run: python -m unittest discover -s tests -v
      - name: Commit release
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add static/app.js public/static/app.js static/style.css public/static/style.css
          git commit -m "Move route date triangle beside day count"
          git rm scripts/apply_triangle_inline.py .github/workflows/apply-triangle-inline.yml
          git commit -m "Remove temporary inline triangle patch files"
          git push
